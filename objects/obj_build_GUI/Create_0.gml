/// @description Insert description here
// You can write your code in this editor

components = array_create(0);

/*
var num_cols = 4;
var spacing = 5;
for (var i = 0; i < array_length_1d(obj_game.components); i++) {
	var x_pos = (x + 100) + spacing * (i % num_cols);
	var y_pos = (y + 100) + spacing * floor(i / num_cols);
	components[i] = instance_create_layer(x_pos, y_pos, GUI_LAYER, obj_game.components[i]);
	components[i].depth = -10000;
}
*/
